---

version: "3"

services:
  postgres:
    image: postgres:12
    restart: always
    ports:
      - "15432:5432"
    environment:
      POSTGRES_DB: "spiral"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"

...
